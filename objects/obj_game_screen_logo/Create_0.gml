// --- Gentle bounce setup ---
base_y = y;          // Remember the starting position
bounce_timer = 0;    // Used for smooth looping
bounce_speed = 0.025; // Speed of the bounce (lower = slower)
bounce_height = 6;   // How high it moves (adjust for subtlety)
