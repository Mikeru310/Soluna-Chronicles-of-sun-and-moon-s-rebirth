//var yy = 100;
//for (var i = 0; i < array_length(global.shop_inventory); i++) {
    //if (mouse_x > 300 && mouse_x < 360 && mouse_y > yy - 5 && mouse_y < yy + 15) {
        //var item = global.shop_inventory[i];
//
        //if (global.gold >= item.price) {
            //global.gold -= item.price;
            //AddItemToInventory(item, 1);
            //show_message("You bought a " + item.name + "!");
        //} else {
            //show_message("You don't have enough gold!");
        //}
    //}
    //yy += 30;
//}
